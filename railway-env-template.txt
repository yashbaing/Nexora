# ─────────────────────────────────────────────────────────────────────────────
# RAILWAY BACKEND ENVIRONMENT VARIABLES
# Copy-paste these into Railway → Your Service → Variables tab
# ─────────────────────────────────────────────────────────────────────────────

PORT=5001
NODE_ENV=production
JWT_SECRET=nexora_secret_key_2026

# Avalanche Fuji Testnet RPC (permanent, never resets)
RPC_URL=https://api.avax-test.network/ext/bc/C/rpc

# Oracle signer wallet (the Fuji deployer wallet)
ORACLE_PRIVATE_KEY=0x81e6a5e00cd5123be27dabf88639c9bd41a8d617c14d1858b26ad162362a54ad

# Your Vercel frontend URL (update after Vercel deploys)
FRONTEND_URL=https://nexora.vercel.app

# PostgreSQL - Railway auto-fills these when you add PostgreSQL plugin:
# PGHOST=<auto>
# PGPORT=<auto>
# PGUSER=<auto>
# PGPASSWORD=<auto>
# PGDATABASE=<auto>
# (or Railway provides DATABASE_URL — see backend db.ts)

# Deployed contract addresses (paste the JSON from deployed-addresses.json as one line)
# After deploying to Fuji, paste the contents of deployed-addresses.json here:
# DEPLOYED_ADDRESSES={"MockUSDC":"0x...","StockwavePlatform":"0x...","chainId":"43113","rpcUrl":"https://api.avax-test.network/ext/bc/C/rpc",...}
