# ArcMOQ Railway / production env
PORT=5001
NODE_ENV=production

# Arc Testnet
ARC_RPC_URL=https://rpc.testnet.arc.io
# Funded deployer/agent private key (never commit real secrets)
PRIVATE_KEY=
AGENT_PRIVATE_KEY=

# After deploy, paste deployed-addresses.json contents or mount the file
# DEPLOYED_ADDRESSES_JSON=
